# Session 28 — 2026-03-15

## Experiment

**Increase Muon ns_steps from 5 to 6 (more Newton-Schulz orthogonalization iterations)**

Rationale: The Muon optimizer uses Newton-Schulz iterations to approximate the polar decomposition for gradient orthogonalization. More iterations = better approximation of the orthogonal matrix, potentially higher-quality gradient updates. The polar*express*coeffs list has exactly 5 entries, so 6 would use all 5. Wait — checking the code, ns*steps indexes into polar*express*coeffs[:ns*steps], and there are exactly 5 coefficients. So ns*steps=6 would just use all 5 (same as ns*steps=5). This means the change had no effect — the result difference is pure GPU variance.

Actually, re-reading the code: `polarexpresscoeffs[:6]` when the list has 5 elements returns all 5 elements. So ns*steps=6 is functionally identical to ns*steps=5. The result difference (1.0492 vs 1.0387) is entirely GPU variance.

## Changes

- `nssteps=5`* → *`nssteps=6` in Muon param group (functionally identical due to coefficient list length)

## Results

| Metric | Value |

|--------|-------|

| val_bpb | **1.0492** ✗ (prev best: 1.0387) |

| Peak VRAM | 22,653 MB |

| Steps | 1,364 |

| MFU | 14.29% |

| Training seconds | 300.2 |

| Total tokens | 178.8M |

| Params | 50.3M |

**Status: DISCARD** — val*bpb 1.0492 vs best 1.0387, regressed by 0.0105; reverted. Note: change was actually a no-op (ns*steps=6 uses same 5 coefficients), so this result is a baseline measurement showing GPU variance.

## Analysis

- The ns*steps parameter indexes *`polarexpresscoeffs[:nssteps]` which has only 5 entries — setting ns*steps=6 is identical to ns*steps=5
- The 0.0105 difference from best is pure GPU/system variance
- This run got 1364 steps at 14.29% MFU — typical for a "slow GPU" allocation
- Session 15's best (1.0387) had 1560 steps at 16.35% MFU — a "fast GPU" allocation
- This further confirms that ~0.01 val_bpb variance exists between different Modal GPU allocations
- To truly add more NS iterations, we would need to add a 6th coefficient tuple to `polarexpresscoeffs`

## Current best config (unchanged)

- Best val_bpb: **1.0387**
- All hyperparameters same as session 27

## Next ideas

- Try Adam beta1 0.8→0.7 — even lower momentum for faster adaptation in short training
- Try lower scalar LR (0.5→0.3) — less aggressive per-layer gating
- Try window pattern "SSL" — shorter pattern cycle, more full-context layers (33% vs 25%)
- Try GQA: n*kv*head=2 with n_head=4 — more efficient attention
- Try wider MLP (4x→6x hidden dim) — richer feature transformations
- Try removing value embeddings entirely — simplification test
- Try softcap as learnable parameter instead of fixed 15
- Try ASPECT_RATIO 64→72 — slightly wider model at same depth